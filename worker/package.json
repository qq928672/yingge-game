{
  "name": "yingge-game-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute yingge-game-db --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute yingge-game-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
